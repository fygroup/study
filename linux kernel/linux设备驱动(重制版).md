### 地址
```
逻辑地址、线性地址、物理地址、总线地址
```

### 总线
```
按照传递的数据分：数据总线（DB-Data Bus）;地址总线（AD-Address Bus）;控制总线（CB-Control Bus）
按照所在区域分（比如计算机）：内部总线，系统总线，外部总线
按实现方式分：并行总线，串行总线

// 信号频次
发信号的频次，我们用赫兹（Hz）这个单位来表示，即（次/秒），1秒一次就是1Hz,1/5秒一次就是5Hz

实际在发数据的时候，并不是以1Hz或是5Hz这样的频率发送的，而是MHz(1000,000Hz)或者GHz(1000,000,000Hz)为单位进行传送的，每次发信号的时间间隔特别小

多个设备使用一条总线，首先是总线占用权的问题，哪一个主设备申请占用总线，由总线仲裁器确定


// 总线的数据传输流程
1、使用总线的总线主设备(如CPU、DMA控制器等)向总线仲裁机构提出占用总线的请求，经总线仲裁机构判定，若满足响应条件，则发出响应信号，并把下一个总线传送周期的总线控制权授予申请者。
2、寻址获得总线控制权的总线主设备，通过地址总线发出本次要访问的存储器和I/O端口的地址，经地址译码选中被访问的模块并开始启动数据转换
3、传送数据总线主设备也叫主模块，被访问的设备叫从模块。主模块和从模块之间的操作是由主模块控制在两个从模块之间通过数据总线进行数据传送。
4、结束主、从模块的信息均从总线上撤除，让出总线，以便其它主模块使用

// 时序信号


```

### io 体系
```
CPU和IO设备之间的数据通路通常称为IO总线

每个IO设备依次连接到总线上，这种连接使用了包含3个元素的硬件组织层次：IO端口、接口、设备控制器

> IO端口
    每个连接到IO总线上的设备都有自己的IO地址集，通常称为IO端口
    
    有4条专用的汇编语言指令可以允许CPU对IO端口进行读写，它们是in、ins、out和outs
    
    在执行其中的一条指令时，CPU使用'地址总线'选择所请求的IO端口，使用'数据总线'在CPU寄存器和端口之间传送数据

    IO端口既可以使用独立的IO地址空间，也可以被映射到物理地址空间，处理器和IO设备之间的通信就可以使用对内存直接进行操作的汇编指令

         -> 控制寄存器 ->
    cpu  <- 状态寄存器 <-  设备IO接口  
         <- 输入寄存器 <-
         -> 输出寄存器 ->

> IO接口


```

### 设备驱动程序模型
```
// kobject
    设备驱动程序模型的核心数据结构，与sysfs绑定在一起，每个kobject对应sysfs文件系统中的一个目录
    包含不同 kobj_type 的 kobject 可以看做不同的子类。通过实现相同的函数来实现多态

// kset
    kobject 的容器，维护了其包含的 kobject 链表


```